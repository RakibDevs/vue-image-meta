/*import axios from 'axios'
import router from '../router'*/


const state = () => ({
    images : (state) => state.images,
    meta   : (state) => state.meta,
})

const getters = {
};


const actions = {
    
}

// mutations
const mutations = {
    

}

export default {
    state,
    getters,
    actions,
    mutations
}